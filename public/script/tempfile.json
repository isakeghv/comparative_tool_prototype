{
    "id": "id-of-study",
    "title": "title of study",
    "description": "description of study",
    "questions": [
        {
            "id": "The ID of the question",
            "question": "The question in text",
            "required": "true/false. deciding if question is required or not",
            "type": "type of question, e.g. multiple-choise, checkbox, range, linear sorting, drag and drop",
            "options": [
                "option 1 - for example drop-box example, or multiple-choise options",
                "option 2 - for example drop-box example, or multiple-choise options",
                "option 3 - for example drop-box example, or multiple-choise options"
            ],
            "artifacts": [
                {
                    "source": "artifact source, e.g file-path",
                    "id": "artifact identifier (for example file-name or one given by researcher)"
                },
                {
                    "source": "artifact source, e.g file-path",
                    "id": "artifact identifier (for example file-name or one given by researcher)"
                },
                {
                    "source": "artifact source, e.g file-path",
                    "id": "artifact identifier (for example file-name or one given by researcher)"
                }
            ]
        },
        {
            "id": "The ID of the question",
            "question": "The question in text",
            "required": "true/false. deciding if question is required or not",
            "type": "type of question, e.g. multiple-choise, checkbox, range, linear sorting, drag and drop",
            "options": [
                "option 1 - for example drop-box example, or multiple-choise options",
                "option 2 - for example drop-box example, or multiple-choise options",
                "option 3 - for example drop-box example, or multiple-choise options"
            ],
            "artifacts": [
                {
                    "source": "artifact source, e.g file-path",
                    "id": "artifact identifier (for example file-name or one given by researcher)"
                },
                {
                    "source": "artifact source, e.g file-path",
                    "id": "artifact identifier (for example file-name or one given by researcher)"
                },
                {
                    "source": "artifact source, e.g file-path",
                    "id": "artifact identifier (for example file-name or one given by researcher)"
                }
            ]
        }
    ],
    "demographicReq": "true/false. Including if demographic questions are required or not",
    "demographic": [
        {
            "id": "id of demographic question",
            "question": "the demographic question",
            "request": "true/false. Deciciding if question should be asked or not (toggle switch/checkbox)",
            "responseType": "handling the type of respons format: multiple choise (radio), text, number etc",
            "options": [
                "option1", "option2", "options to select between if multiple choise"
            ],
            "required": "true/false. Deciding if the question asked is required to answer or not",
            "range": {
                "min": "minimum number, for when choosing 'number' as responseType",
                "max": "max number, for when choosing 'number' as responseType"

            }
        },
        {
            "id": "id of demographic question",
            "question": "the demographic question",
            "request": "true/false. Deciciding if question should be asked or not (toggle switch/checkbox)",
            "responseType": "handling the type of respons format: multiple choise (radio), text, number etc",
            "options": [
                "option1", "option2", "options to select between if multiple choise"
            ],
            "required": "true/false. Deciding if the question asked is required to answer or not",
            "range": {
                "min": "minimum number, for when choosing 'number' as responseType",
                "max": "max number, for when choosing 'number' as responseType"

            }
        }
    ]
}